# AGENTS.md

このリポジトリは、日本語で遊ぶ、完全オフライン対応の単一HTMLクイズアプリです。最終的な利用者向け成果物は `dist/uk_quiz.html` です。

## 必ず守ること

- 最終的なユーザー向け表示は日本語にする。
- UIラベル、クイズタイトル、設問文、回答ラベル、解説、READMEの利用者向け説明は日本語で書く。
- バックエンド、外部API、分析、トラッキング、CDN、リモートフォント、リモート画像を追加しない。
- Service Worker、Web Manifest、PWA専用ファイルを追加しない。
- 最終成果物は、ローカルでHTMLファイルを開いて遊べる単一ファイルにする。
- クイズデータは `src/data/questions.ts` で編集しやすく保つ。
- TypeScriptは読みやすく、小さな関数を優先する。

## アクセシビリティと操作性

- ボタンは実際の `<button>` 要素を使う。
- フォーカス状態を見えるようにする。
- キーボード操作で主要な操作ができるようにする。
- 色のコントラストを保つ。
- SVGや画像選択肢には適切なラベルを付ける。
- 小さなiPhone画面でも読みやすく、大きめのタップ領域を維持する。

## 作業前後の確認

- 変更後は、可能な範囲でビルドと型チェックを実行する。
- `npm run build` が `dist/uk_quiz.html` を生成することを確認する。
- 生成HTMLに外部ネットワーク依存が入らないよう確認する。
